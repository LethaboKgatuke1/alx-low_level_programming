#include "main.h"
/**
 * _isupper - checking if letters are uppercase
 * @y: input to be checked.
 *
 * Return: 1 if y is uppercase
 * 0 otherwise
 */

int _isupper(int y)
{
	if ((y >= 'A') && (c <= 'Z'))
	{


		return (1);
	}
	else
	{
		return (0);
	}
}
