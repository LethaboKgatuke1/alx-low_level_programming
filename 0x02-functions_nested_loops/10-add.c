#include "main.h"
#include <stdio.h>

/**
 * add -prints the addition two integers and returns the result
 * @i: print int i
 * @k: print int k
 * Return: Always 0.
 */

int add(int i, int k)
{
	return (i + k);
}
